#!/bin/bash



filename="myapp.log"

#evalはシェルスクリプトのコード自体を動的に生成して実行する便利なコマンド
#ただし，テキスト文字列をコマンドとして実行するため，OSコマンドインジェクションの脆弱性をもたらす
#外部からの入力値をそのままevalするといった危険な使い方は決してしない

#nオプションは処理後にパターンスペースの内容を出力しないようにするオプション
#pオプションはマッチした場合のみ出力するオプション
#nとpを組み合わせることで，「置換が発生した行のみ出力する」が可能になる

eval $(sed -n "s/<code>\(.*\)<\/code>/\1/p" command.html)



