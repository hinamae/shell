#!/bin/bash

#uptimeコマンドの記録ファイルを定義
uptimelog="uptime.log"

#ヌルコマンドで空ファイルに初期化する
#:=ヌルコマンド
#:のみで使うと空ファイルが作成される．
#:=ヌルコマンド

#touchコマンドでは同様のファイル名があった場合にファイルの初期化はできない．
#: > やcp dev/null などしようすれば空ファイルを作成できる（初期化につかえる）
: > $uptimelog

#10秒おきに6回，uptimeコマンドを実行
#uptime=ロードアベレージを取得
#ロードアベレージ=サーバ上で待ち状態になっているタスクの数を表した数値

#数字自体に意味はない．6この引数を与えることに意味がある
#a b c d e f とかでもいい

for i in 1 2 3 4 5 6
do
 uptime >> $uptimelog
 sleep 10
done
