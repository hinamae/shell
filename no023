#!/bin/bash

#cronなどからスクリプトを振るパスで起動する時，相対パスで起動した時と同じ動きをさせたい時

#cronにhogehoge.shファイルを登録して実行した場合，カレントディレクトリはcronを実行したユーザーのホームディレクトリとなる
#それは実行場所が変わってしまうため，かなりの問題
#それを解決するに，まずcdで自分のファイルの置かれた場所に移動してから処理を開始する


#dirname=フルバスが与えられた際，ディレクトリ部分を取り出すことができる
#$0=シェルが実行された際のコマンド自身を表す変数

cd "$(dirname "$0")"


./no007
./no003
