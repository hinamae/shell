#!/bin/bash

#プロセスID=OS上でプロセス毎に振られる一意の数値
#$$=特殊変数。$$は実行しているシェルのプロセスIDを値として持っている
#同じシェルスクリプトを実行してもそれぞれのプロセスが違う名前で一時ファイルを作成したいときによく使われる手法が拡張子を.$$とする手法
tmpfile="tmp.$$"

#sleepを使用しているため、実行には時間がかかる
#つまり何重にも起動される可能性あり
date > $tmpfile
sleep 10

cat $tmpfile
rm -f tmpfile
