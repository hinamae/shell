#!/bin/bash

data_dir="$HOME/documents/unix_lessons/test"

#ディレクトリ,$data_dir配下のサブシレクトリの容量をサマリ表示(概要，要約表示)する
#du=ファイルがディスク上で利用しているブロックサイズをしめす(ファイルサイズそのものについてではない)
#-kオプション=KB単位表示
#-sオプション=引数で指定した対象のディスクのディスク容量のみを表示
#/*/を指定することによりサブディレクトリごとの容量を表示

#sort=語順にソートする(あいうえお順とか)
#-rオプション=逆順にソート(ディスク使用料の多い順にソート)
#-nオプション=文字列ソートではなく，数値ソートとなるようにしたい場合

du -sk ${data_dir}/*/ | sort -rn

