#!/bin/bash

#テスト用の簡易TCPサーバーをたちあげる

port=8080


#-vオプション=verboseモード　上長モード(macではつかえない)
#-lオプション=リッスンモードで起動する指定(このスクリプトではポート8080で待ち受けるプロセスが起動できる)
#-kオプション=永続化オプション、一度の接続を受け付けることで終わらせるのではなく、何度でも受け付ける(ctl＋cでおわらせよう)
nc -v -k -l $port
