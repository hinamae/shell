#!/bin/bash

#関数を定義したenv.shというファイルを.（ドットコマンドで読み込んでいる)
#ドットコマンドを使用した際は，あたかも読み込まれたソースファイルがドットコマンド位置に貼り付けられたかのような挙動をとる 
#つまり環境変数なども読み込み可能
#(bash env.sh などとしてファイル読み込みをこの箇所で行なった場合別プロセスとして実行されるため，変数や関数定義はもとのスクリプトには反映されない)

. ./env.sh

nowtime

cp -i -v pv-1.4.12.tar.gz "WORK_DIR"

nowtime

